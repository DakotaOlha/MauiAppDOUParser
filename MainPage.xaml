<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="MauiAppDOUParser.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:MauiAppDOUParser.ViewModels"
    Title="DOU Parser">

    <Grid RowDefinitions="Auto, Auto, *">

        <VerticalStackLayout 
            Grid.Row="0"
            Padding="20"
            BackgroundColor="#FF6B35">
            <Label
                FontSize="24"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="Парсер статей DOU.UA"
                TextColor="White" />
        </VerticalStackLayout>

        <VerticalStackLayout 
            Grid.Row="1"
            Padding="15"
            Spacing="10">

            <Button
                Text="Завантажити статті"
                Command="{Binding LoadArticlesCommand}"
                BackgroundColor="#004E89"
                TextColor="White"
                CornerRadius="10"
                HeightRequest="50"/>

            <Label
                Text="{Binding StatusMessage}"
                FontSize="14"
                TextColor="Gray"
                HorizontalOptions="Center"/>
        </VerticalStackLayout>

        <RefreshView
            Grid.Row="2"
            Command="{Binding RefreshCommand}"
            IsRefreshing="{Binding IsRefreshing}">

            <CollectionView 
                ItemsSource="{Binding Articles}"
                SelectionMode="None">

                <CollectionView.EmptyView>
                    <VerticalStackLayout
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        Padding="20">
                        <Label
                            Text="📰"
                            FontSize="60"
                            HorizontalOptions="Center"/>
                        <Label
                            Text="Статті ще не завантажені"
                            FontSize="16"
                            TextColor="Gray"
                            HorizontalOptions="Center"
                            Margin="0,10,0,0"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame
                            Margin="15,10"
                            Padding="15"
                            CornerRadius="12"
                            HasShadow="True"
                            BackgroundColor="White"
                            BorderColor="#E0E0E0">

                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ArticlesViewModel}}, Path=OpenLinkCommand}"
                                    CommandParameter="{Binding Link}" />
                            </Frame.GestureRecognizers>

                            <VerticalStackLayout Spacing="8">

                                <Label
                                    Text="{Binding Title}"
                                    FontSize="18"
                                    FontAttributes="Bold"
                                    TextColor="#1A1A1A"
                                    LineBreakMode="WordWrap"/>

                                <Label
                                    Text="{Binding Description}"
                                    FontSize="14"
                                    TextColor="#666666"
                                    LineBreakMode="WordWrap"
                                    MaxLines="3"/>

                                <Grid ColumnDefinitions="Auto, Auto, *, Auto" ColumnSpacing="15" Margin="0,10,0,0">

                                    <HorizontalStackLayout Grid.Column="0" Spacing="5">
                                        <Label Text="📅" FontSize="12"/>
                                        <Label
                                            Text="{Binding Date}"
                                            FontSize="12"
                                            TextColor="#FF6B35"
                                            VerticalOptions="Center"/>
                                    </HorizontalStackLayout>

                                    <HorizontalStackLayout Grid.Column="1" Spacing="5">
                                        <Label Text="✍️" FontSize="12"/>
                                        <Label
                                            Text="{Binding Author}"
                                            FontSize="12"
                                            TextColor="#004E89"
                                            FontAttributes="Bold"
                                            VerticalOptions="Center"/>
                                    </HorizontalStackLayout>

                                    <HorizontalStackLayout Grid.Column="3" Spacing="10">
                                        <HorizontalStackLayout Spacing="3">
                                            <Label Text="👁️" FontSize="12"/>
                                            <Label
                                                Text="{Binding ViewsCount}"
                                                FontSize="11"
                                                TextColor="Gray"/>
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout Spacing="3">
                                            <Label Text="💬" FontSize="12"/>
                                            <Label
                                                Text="{Binding CommentsCount}"
                                                FontSize="11"
                                                TextColor="Gray"/>
                                        </HorizontalStackLayout>
                                    </HorizontalStackLayout>
                                </Grid>

                                <Label
                                    Text="Читати далі →"
                                    FontSize="13"
                                    TextColor="#FF6B35"
                                    FontAttributes="Bold"
                                    Margin="0,5,0,0"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>